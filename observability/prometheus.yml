global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: parking-system-local
    scheme: http
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8080
        labels:
          environment: local
          service: parking-system-backend
